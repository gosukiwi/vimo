" [git-gutter]

if(exists('g:vimo#layers#gitgutter#disabled') && g:vimo#layers#gitgutter#disabled)
  finish
endif

":setting Show a gutter with the git diff info in buffers
Plug 'airblade/vim-gitgutter'

" vim:ft=vim
