" [Quick Scope]

":setting Makes it easy to see what to target with `f`
Plug 'unblevable/quick-scope'

":setting Only trigger this when using f, F, t or T
let g:qs_highlight_on_keys = ['f', 'F', 't', 'T']

" vim:ft=vim
